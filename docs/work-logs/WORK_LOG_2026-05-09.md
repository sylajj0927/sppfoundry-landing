# Work Log 2026-05-09

## Homepage Section Rhythm Refinement

本輪為首頁 Phase 1.9 之後的第一次視覺精修，基於 `SPP_FOUNDRY_HOMEPAGE_VISUAL_REFINEMENT_PLAN_v1.md` 執行。

### Commit

- `8910bbe` `fix: refine homepage section rhythm`
- Push 成功到 `origin/main`
- Remote update: `63fc6a2..8910bbe main -> main`
- Working tree clean。

---

## 本輪完成項目

### 1. Section label 上方新增短橫線（`.section-intro .kicker::before`）

- 新增 `.section-intro .kicker::before` CSS 規則
- 桌機：`width: 40px`、`height: 1px`、`background: var(--clay)`、`opacity: 0.55`、`margin-bottom: 16px`
- 手機（640px 以下）：`width: 28px`、`margin-bottom: 12px`
- 出現位置：What I Build / Labs / How It Works / Automation / Services / Contact 共 6 個 section kicker 上方
- Hero `.eyebrow` 不受影響（未使用 `.section-intro`）
- About 段不受影響（未使用 `.section-intro`）
- opacity 最終值從原計畫 0.7 調整為 0.55（更含蓄）

### 2. Section intro 桌機間距加大（`.section-intro`）

- `margin-bottom: 38px` → `56px`
- 目標：標題塊與內容塊之間更有獨立性，桌機版閱讀節奏更清楚
- 未動 `grid-template-columns`、`gap`、`align-items`
- 手機版 mobile override（640px）維持 `24px`，不受影響

### 3. Services row padding 增加（`.service-row`）

- `padding: 28px 0` → `32px 0`
- 目標：三條服務列之間更有呼吸感，反映 Services 段的「書頁感」語言
- 未動 `grid-template-columns`、`gap`、`align-items`、`border-bottom`

### 4. Services row 左側短線 alpha 加深（`.service-row::before`）

- `background: rgba(173, 101, 78, 0.42)` → `rgba(173, 101, 78, 0.55)`
- 目標：短線更明確，與新增的 kicker::before 短橫線保持視覺一致性
- 未動 `width`、`height`、`position`

---

## Scope 限制執行紀錄

- 只修改根目錄 `index.html`，未動 `/labs/` 子目錄
- 未改文案（HTML 結構與所有文字內容完全不動）
- 未改 HTML 結構
- 未改 `:root` color tokens
- 未改 button 系統
- 未引入新 SVG / icon / 動畫 / 新色

---

## 部署狀態

- 已 commit：`8910bbe`
- 已 push：`origin/main`
- Cloudflare Pages 自動部署已觸發（由 git push 驅動）
- Working tree clean

---

## 設計決策記錄

- `opacity` 從原計畫 0.7 調整為 0.55：0.7 在淺色背景下略重，0.55 更符合「章節記號而非分隔線」的語意
- Selector 選擇 `.section-intro .kicker::before` 而非裸 `.kicker::before`：避免未來 `.kicker` 套用在 section 外位置（如 Works case study 內頁）時自動長線
- 本輪未動 Hero、Labs 卡片、How It Works 主體、Automation、About、Contact 的任何結構與文案

---

## 後續可評估（不在本輪範圍）

- 確認 Cloudflare Pages 已成功部署後，在桌機 / 手機各 viewport 驗收短橫線位置
- 評估 `section-intro` 空欄（What I Build / Contact 右欄無內容）的桌機版視覺對稱性
- 評估 Hero 背景 `body::before` 格線紋是否需要簡化（下一輪 P1 候選）
- 評估 About 段落加入 Sylvia 名字與短背景句（Phase 2 方向）
